server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /var/www/site;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    access_log /var/log/nginx/linux-server-lab-access.log;
    error_log /var/log/nginx/linux-server-lab-error.log warn;
}
